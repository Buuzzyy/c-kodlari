#include <stdio.h>
int main(){
	
	int x;
	
	printf ("lutfen x sayisini giriniz:");
	scanf ("%d",&x);
	 	
	 	if(x%2==0){
	 		printf ("x degeri %d ciftir",x);
		 }
		 else
		 printf("x degeriri %d tektir",x);
	
        return 0;
}

